# CI-only Docker Compose override for host-level connectivity checks
services:
  redis:
    ports:
      - "6379:6379"
  mlflow:
    ports:
      - "5000:5000"
  redpanda:
    ports:
      - "9092:9092"


